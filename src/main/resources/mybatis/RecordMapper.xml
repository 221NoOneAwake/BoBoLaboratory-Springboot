<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.bobolaboratory.springboot.mapper.RecordMapper">
    <insert id="addRecord" parameterType="cn.bobolaboratory.springboot.entity.Record">
        insert into bobo.record (questionid, studentid, type, date, times, answer, result, score)
            VALUE (#{questionid}, #{studentid}, #{type}, #{date}, #{times}, #{answer}, #{result}, #{score});
    </insert>

    <select id="queryAllRecord">
        select * from bobo.record;
    </select>
</mapper>