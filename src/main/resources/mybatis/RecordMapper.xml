<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.bobolaboratory.springboot.mapper.RecordMapper">
    <insert id="addRecord" parameterType="cn.bobolaboratory.springboot.entity.Record">
        insert into bobo.record (questionId, userId, type, `date`, times, answer, result, score)
            VALUE (#{questionId}, #{userId}, #{type}, #{date}, #{times}, #{answer}, #{result}, #{score});
    </insert>

    <select id="queryAllRecord">
        select * from bobo.record;
    </select>

    <select id="checkTimesByOpenIdAndQuestionId" parameterType="cn.bobolaboratory.springboot.entity.Record" resultType="int">
        select COUNT(id) from bobo.record where userId = #{userId} and questionId = #{questionId} and type = #{type};
    </select>

    <select id="queryRecordByUserId" parameterType="long" resultType="cn.bobolaboratory.springboot.entity.Record">
        select * from bobo.record where userId = #{userId};
    </select>
</mapper>